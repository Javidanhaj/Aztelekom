# Project Tasks (To‑Do List)

This checklist is generated by comparing the current codebase against the requirements in `PROJECT PLAN.txt`. Items are grouped and marked as:
- [x] Done — implemented in the repository
- [~] Partial — exists but incomplete or needs fixes
- [ ] Todo — missing

Each item includes short notes and file references where relevant.

---

## 1) Domain Model

- [ ] Category entity (id, name unique, createdAt)
  - Files: none
- [ ] Product entity (id, name, sku unique, price, stockQty, categoryId, status, createdAt)
  - Files: none
- [x] Customer entity
  - File: `src/main/java/com/aztelekom/internship/domain/entities/Customer.java`
- [~] Order entity
  - File: `src/main/java/com/aztelekom/internship/domain/entities/Order.java`
  - Notes: Core fields are present. Will also require totalAmount calculation logic integration and relations to items.
- [~] OrderItem entity
  - File: `src/main/java/com/aztelekom/internship/domain/entities/OrderItem.java`
  - Notes: `product` relation is commented out; needs relation to Product and business constraints.
- [x] Enums: OrderStatus
  - File: `src/main/java/com/aztelekom/internship/domain/enums/OrderStatus.java`
- [ ] Enums: ProductStatus
  - File exists but no Product entity yet. Ensure alignment once Product is created.

## 2) Repositories

- [ ] CategoryRepository
- [ ] ProductRepository
- [x] CustomerRepository
  - File: `src/main/java/com/aztelekom/internship/repository/CustomerRepository.java`
- [x] OrderRepository
  - File: `src/main/java/com/aztelekom/internship/repository/OrderRepository.java`
- [~] OrderItemRepository
  - File: `src/main/java/com/aztelekom/internship/repository/OrderItemRepository.java`
  - Notes: Uses `CrudRepository`; consider `JpaRepository` for paging/sorting and method `findByOrder_id` -> prefer `findByOrderId` naming.

## 3) DTOs

- [ ] Category DTOs: `CategoryCreateRequest`, `CategoryUpdateRequest`, `CategoryResponse`
- [ ] Product DTOs: `ProductCreateRequest`, `ProductUpdateRequest`, `ProductResponse`
- [x] Customer DTOs
  - Files: `dto/customer/*`
  - Notes: No Bean Validation annotations; add constraints (e.g., `@NotBlank fullName`, `@NotBlank phone`, `@Email email`).
- [x] Order DTOs
  - Files: `dto/order/*`
  - Notes: `OrderCreateRequest` and `OrderItemRequest` have validation annotations; good.

## 4) Mappers (MapStruct)

- [ ] CategoryMapper
- [ ] ProductMapper
- [x] CustomerMapper
  - File: `mapper/CustomerMapper.java`
- [~] OrderMapper
  - File: `mapper/OrderMapper.java`
  - Notes: Maps `OrderItem.product.id` but `product` relation is commented out in `OrderItem`; will fail until Product + relation added.

## 5) Services

- [ ] CategoryService + Impl
- [ ] ProductService + Impl
- [x] CustomerService + Impl
  - Files: `service/CustomerService.java`, `service/impl/CustomerServiceImpl.java`
  - Notes: CRUD-like operations present; `updateCustomer` uses manual mapping, MapStruct method exists but commented in impl; OK to keep manual or switch to mapper.
- [~] OrderService + Impl
  - Files: `service/OrderService.java`, `service/impl/OrderServiceImpl.java`
  - Notes: Interface only exposes `getOrders`; impl is empty. Need full CRUD-like methods per plan: create, get by id (with items), list with filters, status update.

## 6) Controllers (REST Endpoints)

Per plan:
- Orders
  - [ ] `GET /api/v1/orders?status=&customerId=&dateFrom=&dateTo=&page=&size=`
  - [ ] `POST /api/v1/orders` (customerId + items[{productId, qty}])
  - [ ] `GET /api/v1/orders/{id}` (order + items)
  - [ ] `PATCH /api/v1/orders/{id}/status` (NEW→PAID/SHIPPED/CANCELLED)
  - Notes: No `OrderController` file in project.
- Categories
  - [ ] `GET /api/v1/categories`
  - [ ] `POST /api/v1/categories`
  - [ ] `GET /api/v1/categories/{id}`
  - [ ] `PUT /api/v1/categories/{id}`
  - [ ] `DELETE /api/v1/categories/{id}`
  - Notes: No Category code present.
- Products
  - [ ] `GET /api/v1/products?search=&categoryId=&status=&page=&size=&sort=`
  - [ ] `POST /api/v1/products`
  - [ ] `GET /api/v1/products/{id}`
  - [ ] `PUT /api/v1/products/{id}`
  - [ ] `DELETE /api/v1/products/{id}`
  - Notes: No Product code present.
- Customers
  - [x] Basic endpoints implemented
  - File: `controller/CustomerController.java`

## 7) Validation (must)

- [ ] Product validation: name not blank, price > 0, stockQty >= 0
- [ ] Category validation: name min 2 chars, unique
- [~] Order item qty >= 1
  - Present in `OrderItemRequest` (`@Min(1)`); also enforce on entity level or service checks.
- [~] Customer validation
  - Add `@NotBlank` for `fullName`, `phone`; `@Email` for `email` in Customer DTOs. Controller uses `@Valid`.

## 8) Business Rules

- [ ] On order creation, ensure product `stockQty` is sufficient; otherwise 400 error
- [ ] If order is CANCELLED, return stock (bonus)
- [ ] `totalAmount` must be computed on backend (not from client)
  - Notes: Requires Product price + stock management and calculation across items.

## 9) Error Handling (must)

- [ ] Standard JSON error format with a `GlobalExceptionHandler`
  - Suggested files: `exception/GlobalExceptionHandler.java`, `exception/NotFoundException.java`, `exception/BadRequestException.java`

## 10) Security / Config

- [ ] Basic Auth or JWT (ADMIN/STAFF minimal)
  - Suggested: Spring Security configuration under `config/SecurityConfig.java`
- [ ] CORS config (optional but recommended)
  - Suggested: `config/CorsConfig.java`
- [ ] OpenAPI config (optional)
  - Suggested: `config/OpenApiConfig.java`

## 11) Audit Log (bonus)

- [ ] `order_status_history` table/entity to track who changed and when
  - Requires security principal and persistence.

## 12) Data and Persistence Config

- [x] Application properties configured (MySQL + JPA)
  - File: `src/main/resources/application.yaml`
  - Notes: `ddl-auto: update` and `show-sql: true` are set.

---

## Discrepancies with Edited Plan

The plan file lists full `Category` and `Product` modules, but the current repo has only `Customer` and partial `Order` modules. Some items deleted from the plan appear implemented:
- Customer module: implemented (Controller, Service, DTOs, Mapper, Repository, Entity)

---

## Suggested Short-Term Priorities

1) Orders core flow
- [ ] Implement `OrderServiceImpl` methods: create, getById(with items), list with filters, updateStatus
- [ ] Add `OrderController` with endpoints per plan
- [ ] Wire `OrderMapper` after fixing `OrderItem.product` relation
- [ ] Compute `totalAmount` and validate stock against Product

2) Product/Category foundation
- [ ] Create `Category` + `Product` entities and repositories
- [ ] Implement Product stock/price + validations
- [ ] Implement controllers/services + DTOs + mappers

3) Cross-cutting
- [ ] Add GlobalExceptionHandler with standard JSON errors
- [ ] Add validation annotations to Customer DTOs
- [ ] Introduce basic security (Basic Auth) and CORS config

4) Nice to have
- [ ] Audit log for order status history

---

If you want, I can convert this checklist into GitHub Issues with labels and priorities.